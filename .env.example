# Composio API key — get yours at https://app.composio.dev/settings
COMPOSIO_API_KEY=your_composio_api_key_here

# Optional: which providers to triage by default (comma-separated: gmail, outlook)
DEFAULT_PROVIDERS=gmail,outlook

# Optional: how many emails to fetch per run (default 100)
MAX_EMAILS=100
